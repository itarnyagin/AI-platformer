Shit to do:

1. make a variable for Object.size(tiles.tileMap) so that you don't have to call the function for every for() loop instance.
2. for the collision system make a litany of if() statements to chech for multiple conditions of whether it hits and objec from the top, left rihgt og bottom
